module.exports = (function () {
    'use strict';

    var DatumService;

    DatumService = function () {
        var controller = this;

        controller.foo = 'bar';
    };

    DatumService.$inject = [];

    return DatumService;
}());
